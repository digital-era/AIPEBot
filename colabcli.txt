1、powershell执行：
Win + R 这样会以管理员身份启动 PowerShelCtrl + Shift + Enter
  x1@DESKTOP-6HIRFC0
wsl

cd /mnt/e/AIPEQModelSIRIUS

source .colab-env/bin/activate

colab new -s talktonsmgmt

colab drivemount -s talktonsmgmt

# tmux new -s talktonsmgmt

colab exec -s talktonsmgmt -f TalkToNSMgmt.ipynb

# Ctrl+B
  D

#重新打开 JuiceSSH，重新看到运行状态
# tmux attach -t talktonsmgmt

#查看有哪些 tmux：
# tmux ls



#如果你根本不需要实时看过程
nohup colab exec -s talktonsmgmt -f TalkToNSMgmt.ipynb > talktonsmgmt.log 2>&1 &















2、在 WSL 中安装 uv
curl -LsSf https://astral.sh/uv/install.sh | sh
source $HOME/.local/bin/env
uv --version


wsl --status
检查 WSL是否安装成功
进入：
username@computer:~$
uname -x1
应该看到 Linux。



3、安装 Colab CLI
uvx --from google-colab-cli colab --help

4、创建 Colab Runtime&挂载 Google Drive&执行 TalkToNSMgmt.ipynb

创建 Colab Runtime
uvx --from google-colab-cli colab new -s talktonsmgmt
或者为了方便，可以先建立 alias
alias colab='uvx --from google-colab-cli colab'
colab --help
colab new -s talktonsmgmt	

挂载 Google Drive
colab drivemount -s talktonsmgmt

执行 TalkToNSMgmt.ipynb
cd /mnt/e/AIPEQModelSIRIUS
colab exec -s talktonsmgmt -f TalkToNSMgmt.ipynb

完整流程就是：
colab new -s talktonsmgmt

colab drivemount -s talktonsmgmt

cd /mnt/e/AIPEQModelSIRIUS

colab exec -s talktonsmgmt -f TalkToNSMgmt.ipynb

